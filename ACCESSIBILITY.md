# Accessibility

The game can be played entirely with the keyboard: arrow keys hop and Space or Enter starts/restarts. The Canvas has an accessible label, and the touch controls use native buttons with visible text labels. Sound is optional browser output and gameplay feedback is also visual: tile color, score, lives, and state screens are rendered on every frame.
